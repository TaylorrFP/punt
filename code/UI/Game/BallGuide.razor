@using Sandbox;
@using Sandbox.UI;
@inherits PanelComponent

<root>
    <div class="circle"></div>
</root>

@code
{

    [Property] public PuntBall ball { get; set; }

	/// <summary>
	/// the hash determines if the system should be rebuilt. If it changes, it will be rebuilt
	/// </summary>
	
}
